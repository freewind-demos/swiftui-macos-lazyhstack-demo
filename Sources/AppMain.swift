import SwiftUI

@main
struct LazyHStackApp: App {
    var body: some Scene {
        Window("LazyHStack", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 200)
    }
}